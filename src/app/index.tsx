import {Layout} from '../widgets/layout'
import { BrowserRouter } from 'react-router-dom'

function App() {
    return (
        <BrowserRouter>
            <Layout/>
        </BrowserRouter>
    )
}

export default App