import { BrowserRouter } from 'react-router-dom'
import { Layout } from '../widgets/layout'

function App() {
    return (
        <BrowserRouter>
            <Layout />
        </BrowserRouter>
    )
}

export default App